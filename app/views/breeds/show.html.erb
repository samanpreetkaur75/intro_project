<h3>Breed: <%= @breed.name %> <%= @breed.sub_breed.present? ? " - #{@breed.sub_breed}" : "" %></h3>
<h3>Size: <%= @breed.size %></h3>

<h3>Images:</h3>
<div class="row">
  <% @images.each do |image| %>
    <div class="col-md-4">
      <%= image_tag image.url, class: 'img-fluid' %>
    </div>
  <% end %>
</div>
<%= link_to 'Back to Breeds', breeds_path %>
