<h1><%= @breed.name %> <%= @breed.sub_breed.present? ? " - #{@breed.sub_breed}" : "" %></h1>
<h2>Images</h2>
<div class="row">
  <% @images.each do |image| %>
    <div class="col-md-4">
      <%= image_tag image.url, class: 'img-fluid' %>
    </div>
  <% end %>
</div>
<%= link_to 'Back to Breeds', breeds_path %>
